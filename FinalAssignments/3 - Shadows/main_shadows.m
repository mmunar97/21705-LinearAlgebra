%%%%%%%% PARÁMETROS DEL PROBLEMA %%%%%%%%

%domain1 = linspace(i1, i2, K);
%domain2 = linspace(j1, j2, M);
%sun_point = [s1, s2, s3];
%plane_coefficients = [a, b, c, d];

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%surface_points = generate_surface(domain1, domain2);
%shadows = generate_shadows(curve_points, sun_point, plane_coefficients);

%plot_scene(surface_points, sun_point, shadows, plane_coefficients);
