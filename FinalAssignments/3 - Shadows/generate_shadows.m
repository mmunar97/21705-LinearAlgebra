function shadows = generate_shadows(surface_points, sun_point, plane_coefficients)
  [n_rows, n_columns] = size(surface_points);
  shadows = zeros(n_rows, n_columns);
  
  ## COMPLETAR ##
endfunction
